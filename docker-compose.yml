services:
  mongo:
    image: mongo
    restart: always
    ports:
      - "27017:27017"

  app:
    build: .
    ports:
      - "3000:3000"
    env_file: .env
    depends_on:
      - mongo
    volumes:
      - .:/back/app
      - /app/node_modules

volumes:
  mongo-data:
